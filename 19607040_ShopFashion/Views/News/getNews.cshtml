@model IEnumerable<_19607040_ShopFashion.Models.News>
@foreach (var i in Model.Take(3))
{
    @*
        <div class="box-new-container">
            <a href="#">
                <img class="mt-3" src="~/Content/img/@i.img">
                <div class="content">
                    <h3 class="h3">@i.name</h3>
                    <p>@i.description</p>
                </div>
            </a>
        </div>
    *@
    <article>
        <h5 class="text-danger">Mới nhất</h5>
        <div>
            <h3><b>@i.name</b></h3>

            <h4>@Html.Raw(@i.description)</h4>

            <a href="#">Đọc thêm <span>>></span></a>
        </div>
        <img src="~/Content/upload/img/product/@i.img">
    </article>
}